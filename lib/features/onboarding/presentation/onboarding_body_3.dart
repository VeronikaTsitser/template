import 'package:flutter/material.dart';

class OnboardingBody3 extends StatelessWidget {
  const OnboardingBody3({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(); //TODO

    // ImageBody(
    //   image: CustomColors.isDarkTheme ? Assets.images.onboarding3ScreenDark : Assets.images.onboarding3ScreenLight,
    //   title: S.of(context).bringMoonMagic,
    //   description: S.of(context).millionsOfPeopleWorldwide,
    // );
  }
}
