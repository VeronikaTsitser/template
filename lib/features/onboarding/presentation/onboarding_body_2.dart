import 'package:flutter/material.dart';

class OnboardingBody2 extends StatelessWidget {
  const OnboardingBody2({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(); //TODO
    //  ImageBody(
    //   image: CustomColors.isDarkTheme ? Assets.images.onboarding2ScreenDark : Assets.images.onboarding2ScreenLight,
    //   title: S.of(context).dailyPractices,
    //   description: S.of(context).breathingPractices,
    // );
  }
}
